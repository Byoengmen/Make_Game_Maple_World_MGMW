# 게임개발 협업 규칙 (RULES)

> 합의일: 2026-06-17
> 이 규칙은 Windows Desktop ↔ MacBook 어떤 환경의 Claude도 동일하게 따른다.
> 규칙 변경 시 반드시 날짜와 변경 이유를 기록한다.

---

## 1. 세션 규칙

**1-1. 세션 시작 시 Claude는 GAME_CONTEXT.md를 반드시 먼저 읽는다**
- 사용자가 따로 말하지 않아도, 대화가 시작되면 Claude가 자동으로 GAME_CONTEXT.md를 읽고 컨텍스트를 파악한다.
- 파악 후 "현재 Phase N / 마지막 작업 X / 다음 작업 Y" 를 한 줄로 요약해 사용자에게 확인한다.

**1-2. 세션 종료 시 GAME_CONTEXT.md를 반드시 업데이트한다**
- 작업한 내용, 변경된 파일 목록, 현재 파일 구조, 다음 할 일을 기록한다.
- "마지막 작업" 항목은 항상 최신 상태를 유지한다.
- 애매하거나 미완성인 부분도 명확히 기록한다. ("X는 구현했으나 Y는 미완성" 등)

**1-3. 하나의 세션에 하나의 목표만 잡는다**
- 세션 시작 시 "오늘 목표"를 명확히 정한다.
- 도중에 다른 아이디어가 생겨도, 그 세션 목표가 완료되기 전까지 전환하지 않는다.
- 예: "오늘은 웨이브 시스템만 완성한다"

---

## 2. 커뮤니케이션 규칙

**2-1. Claude는 구현 전에 반드시 계획을 먼저 말한다**
- 코드를 바로 짜지 않고, "이렇게 구현하려 합니다 — 맞나요?" 를 먼저 확인한다.
- 사용자가 OK하면 구현 시작.

**2-2. Claude는 모르는 것을 모른다고 말한다**
- MSW API, 특정 동작 방식 등 확실하지 않으면 추측으로 구현하지 않는다.
- "확인이 필요합니다" 또는 "이 부분은 불확실합니다"라고 명시한다.

**2-3. 사용자는 "왜"를 말해준다**
- 변경 요청 시 이유를 함께 말해주면 Claude가 더 좋은 방향으로 구현할 수 있다.
- "이렇게 바꿔줘, 왜냐면 ~" 이 "이렇게 바꿔줘" 보다 더 좋은 결과를 낸다.

**2-4. Claude의 응답은 간결하게 유지한다**
- 불필요한 설명, 반복 요약을 생략한다.
- 코드 + 핵심 설명만 제공. 사용자가 더 알고 싶으면 물어볼 수 있다.

---

## 3. 개발 규칙

**3-1. 한 번에 하나의 기능만 구현한다**
- 기능 A가 완성되고 검증된 뒤에 기능 B를 시작한다.
- 동시에 여러 기능을 건드리지 않는다.

**3-2. 파일 수정 전에 현재 파일을 먼저 읽는다**
- 기존 코드를 파악하지 않고 덮어쓰지 않는다.
- 수정 시 기존 패턴과 스타일을 따른다.

**3-3. 현재 Phase 범위 밖의 기능은 구현하지 않는다**
- Phase 1 중에 Phase 2 기능(가챠, 도감 등)을 미리 만들지 않는다.
- 아이디어가 생기면 GAME_CONTEXT.md 하단 "아이디어 메모"란에만 기록한다.

**3-4. 주요 체크포인트에 log()를 남긴다**
- OnBeginPlay, 중요 이벤트, 분기점에 log()를 추가해 검증 시 확인할 수 있게 한다.
- 검증 완료 후 제거 여부는 사용자가 결정한다.

**3-5. 수치(밸런스)는 상수로 분리한다**
- HP, 데미지, Wave 간격 등 밸런스 수치는 파일 상단에 상수로 몰아둔다.
- 예: `local WAVE_INTERVAL = 5` / `local BASE_HP = 100`

**3-6. 코딩은 사용자가 주도하고, Claude는 조언자 역할을 한다** ★
- 코드의 시작 및 진행은 사용자가 직접 작성하는 것을 원칙으로 한다.
- Claude는 옆에서 방향을 제시하고, 사용자가 막혔을 때 정답을 제시한다.
- 단, 사용자가 "직접 짜줘" 또는 "대신 구현해줘"라고 명시적으로 요청한 경우에만 Claude가 직접 코드를 작성한다.
- 이 작업은 학습과 병행하는 개발임을 항상 염두에 둔다.

**3-7. 완벽히 수행할 수 없는 작업은 시작하지 않는다** ★
- Claude가 요청받은 작업을 "완벽히" 수행할 수 없다는 판단이 들 경우, 작업을 시작하지 않는다.
- 작업 중지 시 반드시 사용자에게 보고한다: "어떤 이유 때문에 이 작업을 중지했는지"를 명확히 설명한다.
- 부분 구현 후 나머지를 흐지부지 넘기지 않는다.

**3-8. "바로 이전 작업으로 돌아가줘" 명령어** ★
- 사용자가 이 명령을 내리면, 가장 마지막으로 요청한 작업을 진행하기 전 상태로 롤백한다.
- 절차: 마지막 작업에서 변경된 파일 목록을 확인 → git으로 해당 파일들을 되돌린다.
- 롤백 전 "이 파일들을 되돌립니다: X, Y, Z — 맞나요?" 를 확인 후 진행한다.

**3-9. 파일 직접 수정 전 반드시 사전 허가를 받는다** ★
- Claude가 파일을 직접 생성/수정/삭제하기 전에 반드시 아래 순서를 따른다:
  1. **완성본 전시**: 수정 후 파일이 어떤 모습일지 전체 내용 또는 변경 diff를 보여준다.
  2. **동작 예측**: 변경 후 어떻게 동작할지 미리 설명한다.
  3. **허가 요청**: "수정을 진행해도 될까요?"로 사용자의 명시적 허가를 기다린다.
  4. **허가 확인 후** 파일 수정 실행.
- 사용자가 "바로 해줘" 또는 "허가 없이 진행해"라고 명시한 경우에만 이 절차를 생략할 수 있다.

---

## 4. Git 규칙

**4-1. 기능 완성 + 검증 후에만 커밋한다**
- Maker에서 Play 후 로그 확인까지 마친 뒤 커밋한다.
- "일단 올려두기" 커밋은 금지.

**4-2. 커밋 메시지는 한국어로, 내용을 명확히 쓴다**
- 무엇을 했는지 한 줄로 명확하게.
- 좋음: `"웨이브 시스템 기본 구현 완료"`
- 나쁨: `"update"` `"fix"` `"수정"`

**4-3. 다른 기기로 이동 전 반드시 push, 이동 후 pull 한다**
- Windows ↔ MacBook 전환 시 동기화를 빠뜨리지 않는다.
- 이동 전: `git add . && git commit -m "내용" && git push`
- 이동 후: `git pull`

---

## 5. 검증 규칙

**5-1. 구현 완료 ≠ 검증 완료. 반드시 Maker에서 실행한다**
- 코드 작성 후 Maker Play → 로그 확인까지 해야 "완료"로 인정한다.
- "아마 될 것 같다"로 넘어가지 않는다.

**5-2. 에러 없음 + 의도한 동작 확인, 둘 다 해야 완료다**
- 빌드 에러가 없어도 실제 동작이 의도와 다를 수 있다.
- log()로 값을 찍어 의도한 동작인지 확인한다.

**5-3. 버그 발견 시 즉시 멈추고 수정한다**
- "나중에 고치자"로 넘어가면 쌓인다.
- 발견 즉시 수정 후 다음 작업으로 이동한다.

---

## 6. 스코프 관리 규칙

**6-1. 작업 중 떠오른 아이디어는 즉시 메모하고 흘려보낸다**
- 좋은 아이디어도 지금 당장 구현하지 않는다.
- GAME_CONTEXT.md 하단 "아이디어 메모"에 적고 현재 작업에 집중한다.

**6-2. Claude가 요청 범위 밖의 기능을 제안하면 사용자가 거부할 수 있다**
- Claude가 "이것도 같이 하면 좋을 것 같아요"라고 해도, 현재 Phase 범위 밖이면 사용자가 "메모만 해두자"로 결정할 수 있다.

**6-3. Phase 변경은 이전 Phase의 마일스톤(M)이 달성된 후에만 한다**
- Phase 1이 미완성인 채로 Phase 2 작업을 시작하지 않는다.

---

## 7. 막혔을 때 규칙

**7-1. 30분 이상 같은 문제에 막히면 접근법을 바꾼다**
- 같은 방법을 반복하지 않는다.
- Claude에게 "막혔어, 다른 방법 있어?"라고 물어본다.

**7-2. Claude도 해결 못하면 솔직하게 말한다**
- MSW 공식 Discord나 문서를 먼저 찾아보고, 그래도 안 되면 "모르겠다"고 한다.
- 억지로 구현하지 않는다.

**7-3. 막힌 기능은 단순화해서 우선 동작하게 만들되, 반드시 나중에 다시 짚는다** ★
- 동작하는 단순한 것 > 동작하지 않는 복잡한 것.
- 단, 단순화한 부분은 GAME_CONTEXT.md에 "미완성/단순화 항목"으로 기록한다.
- 다음 Phase 또는 적절한 시점에 반드시 개선 작업을 진행한다. 잊고 넘어가지 않는다.

---

## 8. 의사결정 규칙

**8-1. 게임 방향, 수치, 기획은 사용자가 최종 결정한다**
- Claude는 제안과 의견을 줄 수 있지만, 결정권은 사용자에게 있다.
- Claude가 임의로 기획을 바꾸지 않는다.

**8-2. 기술적 구현 방법은 Claude가 제안하되, 사용자가 승인한다**
- "이 방법으로 구현하겠습니다" → 사용자 OK → 구현 시작.
- 큰 구조 변경은 반드시 명시적으로 확인한다.

**8-3. 결정된 사항은 GAME_CONTEXT.md에 기록한다**
- 대화로만 결정하고 파일에 안 남기면 다음 세션에 잊혀진다.
- 중요한 결정은 반드시 문서화한다.

---

## 9. 크로스 디바이스 연속성 규칙 ★ (최우선)

**9-1. GAME_CONTEXT.md는 "다른 Claude가 읽어도 동일하게 작업할 수 있는" 수준으로 작성한다**
- 어떤 환경(Windows/MacBook), 어떤 Claude 세션이 읽더라도 현재 상태를 완벽히 파악할 수 있어야 한다.
- 포함해야 할 메타데이터:
  - 현재 Phase 및 진행 상태
  - 구현 완료된 기능 목록 (파일명 포함)
  - 미완성/단순화된 기능 목록과 이유
  - 현재 파일 구조 (전체, 최신 상태)
  - 주요 수치/상수 (밸런스 기준값)
  - 마지막 작업 내용 + 날짜
  - 다음 할 일 (구체적으로)
  - 결정된 사항 (기획, 기술, 방향)
  - 아이디어 메모 (미구현 아이디어)

**9-2. 세션 종료 시 GAME_CONTEXT.md 업데이트는 선택이 아닌 필수다**
- 귀찮아도 빠뜨리지 않는다. 이것이 크로스 디바이스 작업의 핵심이다.

**9-3. Windows ↔ MacBook 전환 체크리스트**
```
이동 전 (보내는 쪽):
  □ GAME_CONTEXT.md 업데이트
  □ git add . && git commit -m "작업내용" && git push

이동 후 (받는 쪽):
  □ git pull
  □ Claude에게 "GAME_CONTEXT.md 읽고 현재 상태 파악해줘" 로 시작
```

---

## 규칙 변경 이력

| 날짜 | 변경 내용 |
|---|---|
| 2026-06-17 | 초안 작성 및 합의 완료 |
